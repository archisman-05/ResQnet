steps:
  - name: 'gcr.io/cloud-builders/npm'
    dir: 'backend'
    args: ['install']

  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t',
      'gcr.io/$PROJECT_ID/backend',
      './backend'
    ]

images:
  - 'gcr.io/$PROJECT_ID/backend'